import React from 'react';
// import pinData from '../helpers/data/pinData';

export default function SingleBoard(props) {
//   state = {
//     pins: [],
//   };

  // componentDidMount() {
  //   const boardFirebaseKey = props.match.params.id;
  //   pinData.getBoardPins(boardFirebaseKey).then((resp) => {
  //       this.setState({
  //         pins: resp,
  //       });
  //   });
  // }
  //   1. Make a call to the API that returns the pins associates with this board.

  //   2. Put the array of pins in state

  // 3. Render the pins on the DOM

  return (
    <div>
        <h1>Single Board</h1>
    </div>
  );
}
